"""Tests for the eval harness foundation (T0032): loader and result schema.

No real API calls are made here — the loader is pure validation, and the schema
tests exercise the Pydantic models directly.
"""
